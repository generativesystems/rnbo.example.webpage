function setup() {
    createCanvas(windowWidth, windowHeight);
    //canvas.position(0,0);
  }
  
  function draw() {
    background(220);
  }

//   This is just to create a background using p5.js to start with